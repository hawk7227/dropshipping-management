import MembershipLanding from '@/components/membership-landing';

export const metadata = {
  title: 'Membership - Pay $0 at Checkout',
  description: 'Join membership and pay $0 at checkout on every order. Access wholesale pricing on all products.',
};

export default function MembershipPage() {
  return <MembershipLanding />;
}
