import AccountPortal from '@/components/account-portal';

export const metadata = {
  title: 'My Account',
  description: 'Manage your membership, billing, and account settings.',
};

export default function AccountPage() {
  return <AccountPortal />;
}
